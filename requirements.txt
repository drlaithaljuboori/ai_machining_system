# requirements.txt - CPU-only version for Railway
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# PyTorch CPU-only (smaller, faster installation)
torch==2.1.2+cpu
torchvision==0.16.2+cpu
torchaudio==2.1.2+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# 3D Processing (lightweight)
trimesh==4.2.3
numpy==1.24.3
scipy==1.13.1

# Optional: Try installing Kaolin from source if needed
# git+https://github.com/NVIDIAGameWorks/kaolin.git@v0.15.0
